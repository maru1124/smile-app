.signup-header
  .header-signup
    =link_to root_path do
      Smile
  %nav.development-bar
    %ul.clearfix
      %li.active
        %p 会員情報
        .circle2
      %li.active
        %p 住所入力
        .circle
      %li.active
        %p 登録完了
        .circle
.main-content
  .sighup-main
    =form_with model: @user, url: {controller: "registrations", action: "create"}, local: true do |f|
      .sighup-content
        %ul.main-title
          %li 会員情報入力
          %li.required 必須
        .sighup-form
          .title
            =f.label :nickname, 'ニックネーム'
          .form
            .text-form
              =f.text_field :nickname, placeholder: "例) 安住スマイル"
        .sighup-form       
          .title
            =f.label :email, 'メールアドレス'
          .form
            .text-form
              =f.email_field :email, placeholder: "PC・携帯どちらでも可"
        .sighup-form
          .title
            =f.label :encrypted_password, 'パスワード'
          .form
            .text-form
              =f.password_field :password, placeholder: "7文字以上の半角英数字", id: "password1"
        .sighup-form
          .title
            =f.label :password_confirmation, 'パスワード(確認用)'
          .form
            .text-form
              =f.password_field :password_confirmation, placeholder: "7文字以上の半角英数字", id: "password2"
        .password-sighup-form
          .checkbox-password
            %input.reveal_password{type: "checkbox"}/
            %i.icon-check
            %label{for:"reveal_password",class: "pass-form"} パスワードを表示する
        .password-info
          %p.password-info-title 
            本人確認
          %p.password-info-text
            安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
        .sighup-form
          .title
            =f.label 'お名前(全角)'
          .form-group
            .text-form
              =f.text_field :last_name, placeholder:"例) 山田", class: "input-half"
              =f.text_field :first_name, placeholder: "例) 太郎", class: "input-half"
        .sighup-form
          .title
            =f.label 'お名前カナ(全角)'
          .form-group
            .text-form
              =f.text_field :last_name_kana, placeholder: "例) ヤマダ", class: "input-half"
              =f.text_field :first_name_kana, placeholder: "例) タロウ", class: "input-half"
        .sighup-form
          .title
            %label{for: "birthday"} 生年月日
            %br/
          .birthday-select-wrap
            .select-wrap
              %i.icon-arrow-bottom
              != sprintf(f.date_select(:birthday, with_css_classes: 'XXXXX', prompt: "--", use_month_numbers: true, start_year: Time.now.year, end_year: 1900, date_separator: '%s'),'年','月')+'日'
        .form-info-text
          ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
        .submit-box
          %p.text
            「次へ進む」のボタンを押すことにより、
            = link_to "利用規約", "#" , target:"_blank"
            に同意したものとみなします
        =f.submit '次へ進む', class: "btn-next", url: "address_path"
= render 'shared/footer'